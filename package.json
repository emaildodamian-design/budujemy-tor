{
  "name": "budujemy-tor",
  "private": true,
  "version": "0.1.0",
  "description": "Co-play turn-based train track PWA for a parent and a young child (offline, no tracking).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^5.9.0",
    "vite": "^8.3.1",
    "vitest": "^5.0.1"
  }
}
